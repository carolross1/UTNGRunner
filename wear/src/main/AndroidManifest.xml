<!-- AndroidManifest.xml del módulo wear -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Wear OS requiere estas features -->
    <uses-feature android:name="android.hardware.type.watch" />
    <!-- Health Services para FC -->
    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <uses-permission android:name="android.permission.health.READ_HEART_RATE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="UTNG Runner"
        android:theme="@style/Theme.WearApp.Round"
        android:supportsRtl="true">

        <uses-library
            android:name="com.google.android.wearable"
            android:required="true" />
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity android:name=".presentation.GameActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
